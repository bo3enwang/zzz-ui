import React from "react";
interface ModalContentProps {
    children?: React.ReactNode;
    style?: React.CSSProperties;
}
declare const ModalContent: ({ children, style }: ModalContentProps) => JSX.Element;
export default ModalContent;
