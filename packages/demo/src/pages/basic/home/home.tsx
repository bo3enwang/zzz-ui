import React from "react";
import { View } from "@tarojs/components";

import { Button, Text } from "zzz-ui";

interface HomeProps {}

export default ({}: HomeProps) => {
  return (
    <View>
      <Button></Button>
    </View>
  );
};
